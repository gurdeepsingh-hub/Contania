Entity settings:
1. All UI in entity settings should be mobile adaptive first and should be more based on cards etc.
2. Currently each item in lists are one per row keep multiple acc to screen sizes.
3. action button are currently with label names consuming much space, keep icons only and add title fields for all actions, user can know name by hover.
4. all create and edit should be popup models based instead of showing up and below.